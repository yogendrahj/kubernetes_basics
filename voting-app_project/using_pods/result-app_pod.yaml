apiVersion: v1

type: Pod

metadata:
  name: result-app-pod
  labels:
    name: result-app-pod
    type: demo-voting-app
  
spec:
  containers:
    - name: result-app
      image: kodekloud/examplevotingapp_result:v1
      ports:
        - containerPort: 80