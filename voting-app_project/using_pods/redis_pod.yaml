apiVersion: v1

type: Pod

metadata:
  name: redis-pod
  labels:
    name: redis-pod
    type: demo-voting-app
  
spec:
  containers:
    - name: redis
      image: redis
      ports:
        - containerPort: 6379