apiVersion: v1

type: Pod

metadata:
  name: voting-app-pod
  labels:
    name: voting-app-pod
    type: demo-voting-app
  
spec:
  containers:
    - name: voting-app
      image: kodekloud/examplevotingapp_vote:v1
      ports:
        - containerPort: 80